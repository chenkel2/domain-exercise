{
  "name": "exercise",
  "version": "0.1.0",
  "private": true,
  "proxy": "http://localhost:3001/",
  "devDependencies": {
    "babel-cli": "^6.23.0",
    "concurrently": "^3.4.0",
    "react-scripts": "0.9.4"
  },
  "dependencies": {
    "react": "^15.4.2",
    "react-bs-notifier": "^4.3.0",
    "react-dom": "^15.4.2",
    "underscore": "^1.8.3"
  },
  "scripts": {
    "start": "concurrently \"react-scripts start\" \"npm run mock-server\"",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "mock-server": "babel-node mock-server.js"
  }
}
